---
tags:
  - daily-worklog
  - journal
date: "2026-09-18"
---
# 🗓️ Worklog: Friday, 18 September 2026

> **Related**: [[Worklogs/Index|📓 Worklogs Index]] | [[Home|🌌 Home]]

## 🎯 Focus & Priorities
- [x] Setup & Migration Flutter SDK ke FVM (Flutter Version Management)
- [x] Konfigurasi Powerlevel10k (p10k) prompt segment untuk deteksi Flutter & FVM version

---

## 💻 Git Activity & Sync Today
- Local system configuration & shell environment tuning (FVM, Zsh, P10k, VS Code).

---

## 📝 Engineering Notes & Scratchpad
- **Flutter SDK FVM Migration & P10k Dynamic Segment**:
  - **Context & Goal**: Menghapus polusi instalasi manual Flutter di host, beralih ke FVM (Flutter Version Management) terstandarisasi, dan menampilkan versi Flutter secara real-time di Powerlevel10k prompt saat masuk ke folder project Flutter.
  - **Changes Made**:
    - Installed FVM 4.3.1 via `leoafarias/fvm` Homebrew tap.
    - Sanitized `~/.zshenv` dan `~/.zshrc`, routing Flutter global default via `$HOME/fvm/default/bin`.
    - Pinned global Flutter SDK `3.44.4` (channel stable) dan configured VS Code User `settings.json` (`dart.flutterSdkPaths`).
    - Configured custom segment `flutter` di `~/.p10k.zsh` dengan ikon Nerd Font `\uE64C` (``) dan foreground color 38. Mendukung FVM v3/v4 config (`.fvm/version`, `.fvm/release`, `.fvm/fvm_config.json`, `.fvm/flutter_sdk`, `.fvmrc`) serta fallback Flutter `pubspec.yaml`.
  - **Verification**:
    - Terminal prompt PTY simulation passed: prompt menampilkan ` 3.44.4` secara otomatis di root dan nested subdirektori project.
    - Benchmark latensi segment: < 0.05 ms per render tanpa subprocess overhead.

---

## 🧠 End of Day Review
- **Key Accomplishments**: FVM migrasi tuntas, shell bersih dari path mati, dan p10k menampilkan versi Flutter secara cerdas & ultra-cepat.
- **Blockers / Follow-ups**: None.
