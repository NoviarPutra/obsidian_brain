---
tags:
  - daily-worklog
  - journal
date: "2026-09-15"
---

# 🗓️ Worklog: Tuesday, 15 September 2026

> **Related**: [[Worklogs/Index|📓 Worklogs Index]] | [[Engineering/Index|⚡ Engineering MOC]] | [[Home|🌌 Home]]

## 📝 Activity & Engineering Notes

- `07:51:17 WIB` **[Voldemort Bot]** Smoke Test: Verified bi-directional vault sync from inside container.
- `15:40:00 WIB` **[Hermes SRE]** Reverted Hermes model configuration on both VPS and local environments back to `AG` (restored from temporary `Agnes`), updated `context_length: 256000` on local config, and restarted `hermes-gateway.service` on `voldemort-vps`. Verified live completion connectivity.
- **Zero-Gap System Hardening Across 5 Core Pillars**:
  - **Memory Consolidation**: Reduced declarative memory usage from 87% down to 31% (693 / 2,200 chars), moving procedural rules to specialized skill references.
  - **Dynamic Skills Hot-Reload & SSOT**: Mounted host `skills_catalog/` into container via `docker-compose.yml`, added `/reload_skills` command for instant reload without image rebuilding.
  - **Proactive SRE Watchdog**: Deployed autonomous monitoring script (`scripts/sre_watchdog.py`) scheduled via Hermes cronjob (`d01d706e7bd2`, every 30m) for instant Telegram alerts on memory (>90%), disk (>85%), or container failures.
  - **Minimalist Seven Dispatch Contract**: Formalized JSON relay contract and validation schemas under `references/autonomous-dispatch-contract.md`.
  - **Bi-Directional Vault Ingestion**: Mounted Obsidian vault into bot container and deployed `vault_scribe.py` to automatically stream admin actions directly into daily worklogs.
  - **Hermes Production Hardening**: Patched `~/.hermes/config.yaml` via CLI (`context_length: 256000`, `compression.threshold: 0.75`, `protect_last_n: 30`, `hard_stop_enabled: true`, `delegation.max_concurrent_children: 3`, `max_spawn_depth: 2`). Verified zero YAML syntax regressions with automatic timestamped backup.
  - **Telegram UX & Output Standardization**: Enforced strict ban on markdown tables in Telegram chats in favor of mobile-friendly labeled bullets and structured cards; locked zero internal tool name leakage into persistent memory.
  - **Obsidian Ecosystem & MCP Integration**: Fully configured [[Templates]] directory with sample note for Templater, enabled JS & Inline JS query engines on Dataview, enabled 11 non-destructive formatting rules on Linter with auto-save active, completed vault markdown hygiene scan (0 trailing spaces, 0 blank line bloat), and linked Obsidian MCP server (`http://127.0.0.1:27123/mcp/`) to Claude Code, Kilo CLI, and Hermes Agent (`mcp_servers.obsidian`).

## sevaka-ui — Time Off Request reskin, tiket 04 (grid + bilah filter)

- **`6a0856fc`** — grid Pengajuan direskin ke prototipe: `.fin-tbar` (status, jenis cuti, "pengajuan saya saja", pencarian karyawan), `.dtable` 6 kolom + kolom aksi berkepala kosong, `.ph-foot`, dan callout kontrak *tanda terima* (`UIC-001-TIME-0.4:407` + `FSD-001-TIME-0.3:506`). Kolom `Employee`/`Extra Layer` tetap nol dibangun — sekarang dijepit tes yang menyuapkan baris yang justru **membawa** field-nya (tiket 08, nunggu BE atas temuan D-5).
- **`b24be6ac`** — kerja tiket 03 yang nyangkut belum di-commit (kerangka `.pg-shell` + bilah dua tab) gue pisahin jadi commit sendiri duluan, biar tiap tiket punya riwayatnya.
- Dua warisan 03 diputus: tombol aksi naik ke `.flow-head__actions` tapi **cuma niat buat-baru** yang ikut naik (`isCreateOpen`), baris yang diubah tetap lokal → satu instans modal, nol dua sumber kebenaran. Hitungan di label tab **ditolak** — harganya seluruh kueri grid ikut naik ke kerangka demi satu pil hiasan.
- `/code-review` dua sumbu nemu satu 🔴: catatan gue ke tiket `05` ngaku ngosongin kepala kolom aksi, padahal dia **sudah** `header: ""` sebelum `b6e31464`. Dikoreksi. Plus sitasi `:478-480` meleset (delapan kunci proyeksi lengkapnya di `478-479`) — dibetulin di enam tempat, tiga di antaranya sudah ada sebelum tiket ini.
- `npm run lint` bersih, suite penuh 3928 hijau. Tes `keepPreviousData` diverifikasi merah dulu tanpa patch-nya.

Terkait: [[sevaka-ui]] · [[Time Off Request reskin]]

## sevaka-ui — Time Off Request reskin, tiket 05 (standar aksi baris)

- **`13fe0073`** — sel aksi grid Pengajuan ikut standar baris prototipe: 1 aksi → `.rowbtn` sebaris, 2+ → kuncup di balik pemicu "Aksi ▾", nol aksi → `.rowbtn--ghost[disabled]` "Tidak ada aksi" (bukan `—` lagi). Gerbang izin nol disentuh satu baris pun — `leaveRequestRowActions.utils.ts` keluar dari diff bersih, jadi AC "perilaku nol berubah" kebukti by construction.
- Berkas tes yang salah nama dipecah: `SevakaLeaveRequestRowActions.test.ts` ternyata **selalu** nguji utils, bukan komponen. Di-`git mv` ke `leaveRequestRowActions.utils.test.ts` (17 kasus utuh + 3 kasus fail-closed), komponennya dapat suite `.tsx` sendiri. Dropdown sengaja **nol** dipalsukan di suite komponen — mock datar bikin tombol sebaris dan item dropdown kelihatan sama, persis beda yang lagi dijepit.
- Premis tes yang gue tulis duluan salah: "baris tanpa `created_by` → tombol nonaktif". Fail-closed malah memperlakukannya sebagai milik sendiri → **buka** Ubah+Tarik. Assertion gerbangnya pindah ke utils.
- Temuan turunan: proyeksi search nol pernah kirim `created_by`, jadi `isOwnSubmission()` **selalu** true di produksi → Setujui/Tolak nol pernah muncul di grid sama sekali, dan Ubah/Tarik ditawarkan di baris orang lain. Kebalikan `FSD-001-TIME-0.3:301`. Nol diperkenalkan tiket ini; dicatat di tiket 08 sebab obatnya jawaban BE atas D-5.
- `/code-review` dua sumbu nemu 2 🔴 — dua-duanya sitasi gue sendiri: ternary `acts.length > 1` ternyata milik `js/finance-disbursement.js:49`, bukan `flow-common.js`; dan tombol nol-aksi prototipe **bisa diklik**, bukan nonaktif. 4 temuan ditolak beralasan.
- `npm run lint` bersih, suite penuh 3935 hijau. Empat kasus komponen diverifikasi merah dulu.

Terkait: [[sevaka-ui]] · [[Time Off Request reskin]]

## sevaka-ui — Time Off Request reskin, tiket 06 (modal formulir + modal detail)

- **`97297709`** — kedua modal ikut bentuk overlay prototipe: kepala bernada (`.ovl__head`), badan satu-satunya yang menggulir, kaki tetap berisi aksinya (`.ovl__foot`). Badan modal detail jadi grid `.kv`, dan sub-view Riwayat Akses dikeluarkan dari grid itu — rumahnya tetap di sini (`FSD-001-TIME-0.3:304`/`:550`, dua-duanya gue cek ke dokumennya dan **benar**, beda dari beberapa tiket sebelumnya).
- Temuan utama: `.ovl__body` — satu-satunya aturan yang bikin "hanya badan yang menggulir" — **nol pernah masuk** kulit kita. Markup prototipe nol nulis kelasnya; `standardizeModals()` (`js/flow-common.js:173`) yang nyuntik saat runtime, dan berkas itu justru yang `spec.md` tolak diangkut. Kelas kelima dari keluarga yang sama (empat pertama dicatat tiket 01). Yang nyelametin AC: `SevakaBaseModal` **sudah** kasih bentuk itu sendiri — nol nambah CSS, dan emang nol boleh, stylesheet-nya keluaran filter.
- Jebakan portal: `DialogContent` merender lewat `DialogPortal` ke `document.body`, jadi pembungkus kulit di halaman **nol pernah** nyampe isi modal. Kelasnya dipasang di panel, konstantanya diimpor dari modul scope biar efek samping `import`-CSS ikut kebawa. Dua hal gue verifikasi bukan asumsi: aturan `.sevaka-time-off-skin {}` telanjang isinya **cuma** kustom properti `--tor-*` (nol deklarasi visual → inert buat panelnya), dan nol satu pun nama kelas kulit dipakai di `src/**/*.tsx` luar `time-off`.
- Mindahin kaki keluar dari `<form>` ngelahirin cacat sendiri: kaki tetap kepasang selagi rangka muat mode Ubah tampil, padahal `<form id>` sasaran atribut `form` belum ada → Simpan bisa diklik tapi mandul. Ditambal (disabled) + ada tesnya. Sebelumnya mustahil, sebab rangka muat dulu ngeganti formulir **berikut** tombolnya.
- AC 6 intinya justru yang **nol** dibangun: panel turunan prototipe ngitung `total_days` + empat gerbang di sisi klien dari data palsu. Yang diambil cuma chrome `.tm-derived`; isinya satu baris jujur, dan tesnya njepit **ketiadaan** (`passed`/`blocked`/`Gerbang` nol boleh muncul).
- `/code-review` **nol jadi jalan** — dua sub-agennya mati kena batas laju sesi (429) sebelum ngeluarin apa pun. Review gue kerjain sendiri; satu temuan ketangkep dan ditambal (varian tombol Tutup beda sama saudaranya Batal).
- `npm run lint` bersih, suite penuh **3944 hijau** (dari 3935, +9). Kesembilannya diverifikasi merah dulu.

Terkait: [[sevaka-ui]] · [[Time Off Request reskin]]

## sevaka-ui — Time Off Request reskin, tiket 07 (tab Delegasi)

- **`34fcaa69`** — permukaan Delegasi ikut prototipe: `.sec-head` (judul + catatan + tombol "Daftarkan Pengganti"), tabel jadi `.dtable` polos di dalam `.dtable-wrap--scroll`, kosongnya `.tempty` **di dalam** tabel, galat jadi `.note--warn`, plus callout kontrak tiga kalimat yang masing-masing berjangkar sitasi.
- Temuan terbesar justru **bug gerbang**, bukan kulit: `canCancelDelegation` berbunyi `!== CANCELLED && !== REJECTED`, jadi delegasi **Disetujui** nawarin Batalkan yang server tolak. Kontraknya naruh ambang `PENDING_APPROVAL` **tiga** kali (`UIC-001-TIME-0.4:629`, `FSD-001-TIME-0.3:584`, plus `:562`/`:571` di flowchart-nya yang justru ketemu sub-agen review). Dua gerbang dilebur jadi satu `canManageDelegation`.
- Ikutannya: cabang satu-aksi standar tiket 05 jadi **nol terjangkau** di tabel ini, jadi selnya cukup ternary dua bentuk — persis bentuk prototipe-nya sendiri (`js/time-off-request.js:130-135`). Nol bikin komponen aksi baris kedua; tiket 05 sudah memutuskan "pinjam bentuknya, bukan komponennya", jadi temuan *Duplicated Code* dari sumbu Standards gue tolak pakai kata-kata tiketnya sendiri.
- Dua kolom prototipe nol dibangun, sebab proyeksi `POST /leave-delegations/search` cuma lima kunci (`UIC-001-TIME-0.4:613-614`): `Cakupan` nol punya sumber sama sekali, `Delegator` punya field tapi nol punya nama. Dicatat ke tiket 08 satu meja sama D-5 — plus temuan turunan: `POST /employees/search` DTO-nya nol nerima id sama sekali (`UIC-001-EMPLOYEE-0.9:217-224`), jadi `employee_id` yang D-5 minta pun berhenti sebagai uuid di layar kalau nol ada pintu resolusi per-id.
- Label ikut FSD, bukan terjemahan tiket: "Ubah Pengganti"/"Daftarkan Pengganti" (`FSD-001-TIME-0.3:306-307`), bukan "Ganti Pengganti" yang tiket usulkan.
- `/code-review` dua sumbu jalan penuh kali ini. Sumbu Spec nemu 1 🔴 nyata: sel "Cuti yang Didelegasikan" gue melukis `— · lr-1` pas induknya nol ketemu — uuid yang tiket ini sendiri sebut cacat. Prototipe naruh tanda hubung telanjang di situ (`js/time-off-request.js:138`). Ditambal. Sitasi diaudit satu-satu: **nol yang meleset**, pertama kalinya di seri ini.
- `npm run lint` bersih, suite penuh **3950 hijau** (dari 3944, +6). 14 dari 17 kasus di dua berkas tes yang ditulis ulang diverifikasi merah dulu.

Terkait: [[sevaka-ui]] · [[Time Off Request reskin]]

## 📝 Engineering Notes & Scratchpad
- **[12.30] Hermes Gateway VPS Model Configuration**:
  - Updated default model in `voldemort-vps:~/.hermes/config.yaml` from `AG` to `Agnes` (OmniRoute combo upstream `agnes/agnes-2.5-flash`).
  - Gracefully restarted `hermes-gateway.service` on VPS and verified zero-downtime activation and upstream latency.
- **[13.10] Local Hermes Ecosystem Alignment to VPS Parity**:
  - Purged local Mac bloat (800+ lines generated custom provider stubs, unused platform profiles).
  - Aligned local `~/.hermes/config.yaml` 1:1 with VPS SSOT (`compression` threshold 0.75 / protect 30, `tool_loop_guardrails` hard stop enabled / failure count 6, `delegation` max 100 / depth 2, model upstream `AG` aligned with live VPS config).
  - Preserved essential MCP endpoints (`obsidian` local REST API and Google `stitch` MCP).
  - Maintained zero touch on Kilo Code configuration (`~/.config/kilo/`, `~/.kilocode/`).
- **[12.05] Claude Code Session (`sevaka-ui`)**:
  - **Working Directory**: `/Users/pt-dika/Dika/sevaka-ui`
  - **Recent Commits**:
    - `34fcaa69 feat(time-off): reskin the Delegasi tab to the prototype`
    - `97297709 feat(time-off): reskin the request form and detail modals`
    - `13fe0073 feat(time-off): adopt the prototype row-action standard`

## Time Off reskin — tiket 08 (terblokir → koreksi dokumen)

`2531ec97` · branch `feat/time-september`. Tiket 08 (`Employee`/`Extra Layer`) nunggu jawaban BE, jadi kerjanya koreksi dokumen: tiket 08, adendum `A-1`..`A-5` di `be-handoff-time-02.md` (append-only), issue [[time-integration]] 07 `resolved` → `needs-info`, plus `map.md` yang sempat ngaku "sebelas dari sebelas tuntas".

Temuan: `AC 2` nyitir `FSD:408` buat kolom grid padahal itu peta komponen **formulir**; permintaan ke BE naik dari 2 field jadi 3 sebab `canSickReject` mati dua kali (`isOwnSubmission` **dan** `reject_deadline_at` sama-sama di luar 8 kunci proyeksi); dan `T-4` ternyata `resolved` di atas mekanisme yang DTO-nya nol bisa mengungkapkan.

Catatan buat diri sendiri: verifikasi gue sendiri lahirkan 2 angka salah (pembagi dari teks heading, bukan dari endpoint path) — `/code-review` dua sumbu yang nangkep sebelum kekirim. Bukti terkuat ternyata ada di balasan BE (`feedback:277/283/313`), bukan di handbook.

## sevaka-ui — Header profil-diri: nama pindah ke `/auth/me`

- Branch `feat/profile-september`. Nama di header `SevakaBasicInfoIdentitySummary` tadinya diambil dari `useSevakaEmployeeDetail` (direktori karyawan); sekarang dari `useSevakaAuthStore((s) => s.fullName)` — payload `/auth/me` yang emang udah ditulis `setIdentity()` pas login/initializer. Nol fetch baru, nol impor baru.
- Cakupan dinaikin dari 1 route ke **7 halaman** profil-diri (BasicInfo, Additional, EmergencyContact, Family, FormalEducation, InformalEducation, WorkingExperience) — semuanya pake pola `name={identity?.name}` yang sama. Kalau cuma satu yang pindah, nama bakal keliatan ganti-ganti pas user pindah tab. `BasicInfoManagePage` (`/:employeeId/`) nol render header ini, jadi nol ada jalur `/auth/me` nampilin nama orang lain.
- Foto tetep `profileQuery.photo_url` (payload `/auth/me` nol bawa `current_photo`), dan posisi/cabang/NIK/kewarganegaraan/status tetep dari direktori + profil. Gerbang `isLoading` nol disentuh — masih jujur buat 5 field itu.
- Tes: mock direktori di 7 berkas diganti namanya jadi `"Nama Direktori"` sementara store diisi `"Budi Santoso"`, jadi tesnya beneran mati kalau sumbernya balik ke direktori. 7 merah diverifikasi dulu, abis patch `npm run lint` bersih + suite penuh **3925 hijau**.

Terkait: [[sevaka-ui]] · [[Employee Profile]]
- `15:42:00 WIB` **[Hermes Model Switch]**: Reverted default model from `Agnes` to `AG` in `/home/voldemort/.hermes/config.yaml` with backup (`config.yaml.bak.20260915083720`). Restarted `hermes-gateway.service`, verified telegram polling active and config loaded cleanly with `AG`.

## AUTH wayfinder tiket 02 — status gerbang `/auth/me` (resolved)

- Cabang **(a)**: `GET /api/v1/auth/me` **ber-route**. Dibuktikan probe DEV `hris-dev.dikahadir.com` tanpa kredensial — `401 "Missing JWT token in request"`, sementara kontrol `GET /auth/login` balas `404 "Alamat tidak terdaftar di gateway"`. `FSD-001-AUTH-0.7` (14 Sep) dan `UIC 0.14` udah dibantah server sejak **2026-09-08**. Sembilan temuan blocker lepas; gerbang keras `fetchMe()` di login sah.
- Aturan baru buat seluruh peta: **baris status handbook nol boleh jadi bukti realisasi — yang buktiin cuma probe.** Sisa satu butir: lapisan terluar muatan (amplop STD + `timestamp`) nol pernah dilihat → tetap Q16, dirutekan ke tiket 03 butir 4 + tiket 12.
- Temuan sampingan: dokumen handoff BE AUTH (Q1–Q16) ternyata **udah ada** di branch `dev-voldemort` (`b3a4daa7`), nol pernah masuk `feat/auth-september` — premis tiket 12 direvisi jadi tiga keputusan, bukan "tulis dari nol".

Terkait: [[sevaka-ui]] · [[AUTH]]

## AUTH wayfinder tiket 03 — sumber identitas `/auth/me` (resolved)

- **Gerbang keras dipertahankan di jalur login + reload, tapi alasannya diganti.** Docblock lama bilang "one source of truth" — itu preferensi. Alasan sebenarnya dua, keduanya struktural: `role` nol punya sumber lain di reload (`POST /auth/refresh` bawa `company_code` tapi **nol** bawa `role`, UIC §3.3 — udah tercatat ke BE sebagai **Q12**, belum dijawab), dan `employee_id` nol punya sumber lain titik (Jalan A `PROB-SERVICE-344`, Jalan B **ditolak**), dengan **24 call site** produksi di 5 domain.
- **Empat premis tiket meleset, dan fakta penentunya justru sitasi yang nol ada di tiket.** Klausa degradasi FSD §2.9 H1b bersyarat _"sampai route dibuat"_ (syarat udah kedaluwarsa), UIC §10.3 butir 4 contohnya **jabatan** bukan `/auth/me`, alasan sumber-tunggal **tercatat** di UIC §10.1 + dua docblock, dan §10A.3 sekarang **arsip keputusan** bukan butir terbuka.
- 🔴 **Cacat produksi lahir dari verifikasi**: `isSevakaApiEnvelope` nuntut 4 kunci (`data`+`status`+`code`+`timestamp`); amplop `/auth/me` yang UIC cetak sendiri cuma 3 — nol `timestamp`. Jadi respons yang **100% patuh kontrak pun ditolak**, muatan diserahin utuh di-`as`, semua field mendarat `undefined`. Gejala "nama nol tampil" yang seminggu ini disalahin ke server (**Q16**) ternyata separuh milik FE; baris "kode frontend salah membaca — Gugur" ditarik.
- **Probe kedua**: ketiga alamat "data saya" + menu-tree semuanya ber-route (`401` diskriminatif, **4 kontrol** balas `404` bentuk gerbang). Sampingan: `COMPANY999/employee/me` → `404`, bukan `403` — awalan tenant diresolve per company code **ter-provision**, gagal sebelum JWT dibaca.
- `dev-voldemort` **ditinggalkan** (keputusan user). Handoff doc 341 baris / 16 pertanyaan diselamatkan ke `feat/auth-september` + addendum bertanggal. Frontier peta naik **7 → 8** (03 melepas 10 dan 11). Nol kode produksi disentuh — peta ini planning.

Terkait: [[sevaka-ui]] · [[AUTH]]

## AUTH butir build mendesak #3 — penjaga amplop (DONE, `50b7b915`)

- **Penjaga nolak bentuk yang sah.** `isSevakaApiEnvelope` nuntut 4 kunci (`data`+`status`+`code`+`timestamp`); amplop yang handbook cetak punya **3**. Jadi respons **patuh kontrak pun ditolak**, diserahkan utuh, di-`as T`, semua field mendarat `undefined` — nol galat, nol jejak. Itu sebab nama pemakai nol pernah tampil.
- **Bukan bug `/auth/me` — bug 3 alamat.** `UIC-001-EMPLOYEE 0.27 §1.5:157`: amplop `{status, code, data}` = _"bentuk **beku** yang sama untuk **ketiga** alamat 'data saya' lintas service"_, pengecualian bernama `PROB-SERVICE-291`, diputus principal 31 Juli 2026. Satu guard matahin `/auth/me` + `/employee/me` + `/company/my-position`.
- **Blast radius diukur, bukan dikira.** Interceptor emang global ([client.ts:135]), tapi dari 5 bentuk berkontrak cuma **1** yang flip perilaku (amplop beku). STD 5 kunci tetep dibongkar (superset), muatan datar §3–§5 tetep lewat, grid §11.2 + array telanjang tetep lewat.
- 🔴 **Hijau bisa bohong**: `client.test.ts` nge-mock `axios` cuma `create`+`post`. `fetchMe` pakai `axios.get` → **nol pernah dieksekusi tes mana pun**, padahal berkas tesnya 600+ baris. Dan dugaan "kode FE salah baca" di Q16 digugurkan pakai tes yang hidup di `dev-voldemort`, nol pernah masuk branch berjalan.
- **Yang dikerjakan**: guard turun ke 3 kunci; `isSevakaMeIdentity` fail-closed dipasang di `fetchMe`; `unwrapApiEnvelope.test.ts` lahir (nol ada sebelumnya) ngunci 5 bentuk; `client.test.ts` dapat mock `axios.get` + 7 tes penjaga; 3 docblock "sole source of truth" diganti 2 alasan struktural + syarat pencabutannya; 2 docblock ngutip `ADR-0001`/`ADR-0002` yang **nol ada** di repo; 4 docblock sibling yang bilang alamatnya belum ber-route dikoreksi.
- `lint` + `tsc -b` bersih. Suite penuh **3931 hijau / 11 merah** — sebelas-duanya `Test timed out in 5000ms` di `employee-profile/basic-info` yang nol nyentuh permukaan berubah, dan **153/153 hijau pas dijalanin sendirian**. Flake beban paralel, bukan regresi.

Terkait: [[sevaka-ui]] · [[AUTH]]

## AUTH wayfinder tiket 04 — Menu Unlock User (resolved + cacat dibunuh)

- **Delapan keputusan, empat premis tiket dikoreksi.** Kartu tayang **kosong-berketerangan**: opsi "sembunyikan" mati karena FSD §7 (register elemen yang harus dibuang) isinya tiga baris dan **Unlock User nol ada di situ** — menu ini di dalam kontrak.
- **Butir 2 ternyata keputusan lingkup, bukan bentuk endpoint.** Kedua kandidat alamat yang tiket tawarkan sama-sama `404` di probe; sapuan lima handbook September non-AUTH nol nemu layanan yang ngeklaim permukaan ini. Satu-satunya alamat hidup yang nyentuh `log_login_attempt` duduk di **§8**, yang peta sendiri taruh di luar lingkup.
- **Butir 4 nol butuh TSD.** `UIC:1031` ngasih §8 ke persis dua peran yang FSD tulis dengan izin `audit:read` → gerbang tampil-layar duduk pada izin atas **data**, bukan atas **aksi**. Kode nol berubah; alasannya yang berubah.
- 🔴 **Cacat dibunuh (`ac0ede5d`)**: `useSevakaLockedAccountsQuery` nol punya panggilan jaringan — dia ngembaliin fixture dua baris bernama orang, tayang di **sebelas** path route, nyuapin tombol yang nembak `POST /auth/unlock-account` **sungguhan**. Ikut dicabut: penyembunyian baris kedaluwarsa sisi-klien (bikin pencacah bohong), toast yang ngeklaim "User dapat login kembali" (dilarang UIC `:649` — alamat itu cuma buka satu lapis), dan `fullName` yang jadi field tulis-saja.
- **Empat berkas tes mensertifikasi perilaku lama**; `[ui-7]` **dibalik**, bukan diperbarui — sekarang dia negesin baris kedaluwarsa tetap dirender dan pencacahnya cocok.
- **Probe**: `POST /audit/login-attempts/search` **ber-route** → **handoff Q13 gugur**. `audit/*` nol ber-prefiks tenant.
- `lint` + `tsc -b` bersih, **319 tes hijau**. Tiket build **13** lahir (terblokir balasan BE atas Q2). Frontier peta tujuh: 05-11.

Terkait: [[sevaka-ui]] · [[AUTH]]
