---
tags:
  - daily-worklog
  - journal
date: "2026-09-24"
---
# 🗓️ Worklog: Thursday, 24 September 2026

> **Related**: [[Worklogs/Index|📓 Worklogs Index]] | [[Engineering/Index|⚡ Engineering MOC]] | [[OmniRouter/Index|🔀 OmniRoute Hub]] | [[Home|🌌 Home]] | [[Worklogs/2026-09-23|◀ 2026-09-23]]

- **[00.20] sevaka-ui — Integrasi balasan backend MYTHOS (handoff 23 Sep 19:12)**:
  - **Sumber**: `docs/handbooks/SEPTEMBER-22092026/HANDOFF/noviar-feedback-2609231912-analisa-mythos.md` (read-only). Branch `september`, belum di-commit.
  - **Lintas modul**: hook baru `src/shared/hooks/useSevakaEmployeeNames.ts` — satu `POST /employee/employees/search` ber-`employee_ids[]` per 100 id; label nama → NIK → "—", tidak pernah uuid mentah. Dipakai Attendance, Time Off, Overtime/On Call.
  - **TIME**: ATT-3 swafoto kamera (stream `track.stop()` di semua jalur, JPEG ≤1280 px), ATT-5 16 layar tanpa kontrak dihapus, ATT-2 HR_STAFF koreksi atas nama karyawan, CAL-2 "Simpan & Ajukan", PRJ-2/3/5/7/11/13 kolom proyeksi, OT-3 saring `oncall_assignment_id` di klien, REQ-2 `has_doctor_note` tidak dikirim.
  - **Employee Directory**: DIR-1 email dicabut, DIR-3 `job_grade_name` dari server, DIR-2/4 pesan eksplisit saat lookup `403`.
  - **Finance/Payroll/Aset**: TGL-3 validasi tanggal cash advance (`>=` buat, `>` ubah), TGL-4 lima layar payroll faker → placeholder, AST-6 `notes`+`AUCTION` dicabut, AST-2 kunci foto dihilangkan, AST-5 panel impor aset tahap 1.
  - **Catatan**: pelacak tunggal `.scratch/handoff-2026-09-23/be-reply-tracker.md` (status per butir + TASK BE + 14 pertanyaan putaran 2 R2-1..R2-14); banner penunjuk di 4 berkas handoff; tiket 31 resolved; catatan payroll & directory diperbarui.
  - **Perbaikan tambahan**: tes `sevakaNewJoinerValidation.test.ts` gagal pukul 00.15 WIB — `toISOString().slice(0,10)` memberi tanggal UTC kemarin. Diganti `toApiDate`; pola sama di `sevakaReprimandValidation.test.ts`.
  - **Verifikasi**: `tsc -b --noEmit` bersih, `eslint .` bersih, Prettier bersih pada berkas berubah, vitest penuh 4674/4675 (sisa 1: `SevakaEmployeeRoleGate.test.tsx`, gagal sejak sebelum sesi).
  - **Terbuka (keputusan USER)**: `queryClient.clear()` saat logout (cache lintas pengguna di tab sama; swafoto tertahan 24 jam di mutation cache), layar payroll faker lain (R2-14), kolom Karyawan di Riwayat Tap HR.

- **[00.41] Core Architecture — Penerapan BOZAGENTIC_GEMINI_CORE untuk OpenCode & Kilo**:
  - **Tindakan**: Menerapkan arsitektur dan sistem direktif `BOZAGENTIC_GEMINI_CORE.md` (Gemini 3.8 Flash Edition) dari Telegram Desktop ke runtime OpenCode dan Kilo CLI.
  - **Single Source of Truth**: Menyimpan master manifest di `/Users/pt-dika/Documents/Obsidian/Engineering/BOZAGENTIC_GEMINI_CORE.md` dan menambahkan referensi `@BOZAGENTIC_GEMINI_CORE.md` pada direktif utama `/Users/pt-dika/Documents/Obsidian/Engineering/CLAUDE.md`.
  - **Konfigurasi Engine**: Menginjeksi properti `instructions` ke `~/.config/opencode/opencode.json` dan `~/.config/kilo/kilo.json` mengarah ke file master engineering tersebut.
  - **Verifikasi**: `opencode debug config` dan `kilo debug config` memvalidasi parsing instruksi tanpa schema breaking. Backup timestamped diamankan di masing-masing direktori.

- **[00.50] sevaka-ui — Tindak lanjut keputusan USER atas tiga butir terbuka (entri 00.20)**:
  - **Logout**: `queryClient.clear()` di `logout()` pada `src/shared/stores/useSevakaAuthStore.ts` (titik tunggal kelima jalur keluar). Membuang cache query + variabel mutasi (swafoto) dan membatalkan fetch berjalan (diverifikasi di sumber `@tanstack/query-core`). Tes baru di `useSevakaAuthStore.test.ts`.
  - **Riwayat Tap HR**: kolom Karyawan; satu `useSevakaEmployeeNames` untuk Riwayat Tap + Ringkasan, ditunda sampai kedua panel mendarat (satu lookup).
  - **Payroll TGL-4b**: Riwayat Payroll, GL Mapper, Cost Center, BPJS New Rate, PPh 21 DTP dicabut → placeholder (nol API, nol kontrak, diverifikasi per konsep termasuk istilah Indonesia). Kode faker + `components/shared/SevakaAssignEmployee{Modal,FilterPanel}.tsx` yatim dihapus.
  - **Root cause tambahan**: seluruh tautan `/admin/payroll/...` jatuh ke 404 (rute payroll di akar, cabang `admin` hanya dashboard). Diperbaiki ke `/payroll/...`; penjaga regresi baru di `src/routes/routes.test.ts` (glob `?raw`, pola dibaca dari pohon route; uji mutasi terbukti gagal saat tautan rusak dipasang ulang).
  - **Verifikasi**: `tsc -b --noEmit` bersih, `eslint .` bersih, Prettier bersih, vitest penuh 4683/4684 (sisa 1 pra-sesi: `SevakaEmployeeRoleGate.test.tsx`).

- **[01.20] sevaka-ui — Tata letak Dashboard (HOME) responsif desktop & mobile**:
  - **Root cause**: breakpoint viewport (`md:`/`lg:`) dipakai untuk area yang lebarnya dipotong sidebar 316px. Viewport 1024px → konten ±660px, tetapi layout sudah mode `lg` (ilustrasi banner dipaku 460px, judul 56px) → judul pecah 5 baris, teks promo terpotong. Di 768px kartu statistik hanya mengisi separuh kiri; di ≥1280px kartu bertumpuk di kolom 280px meninggalkan ruang kosong besar.
  - **Fix**: container query Tailwind v4 (`@container` di `SevakaDashboardPage`, `@container/promo` di promo). Banner teks-dulu, berdampingan mulai konten 576px; kartu statistik jadi grid 1 → 2 → sebaris (`auto-fit`, dua kartu EMPLOYEE melebar penuh); kartu Keamanan Akun + promo berdampingan mulai 768px, wrapper `empty:hidden` bila kartu tergerbang `null`. Slot `midColTop` → `actionCard`. Ilustrasi `alt=""` (dekoratif).
  - **Verifikasi visual**: harness sementara + Chrome headless di 375 (via iframe; jendela headless min ±500px), 768, 1024, 1280, 1440, 1920 untuk HR_MANAGER, EMPLOYEE, dan keadaan kosong. Harness dihapus.
  - **Verifikasi**: `tsc -b` bersih, ESLint bersih, Prettier bersih, 75/75 tes dashboard.

- **[01.05] Obsidian Vault Architecture & Zero-Gap Graph Hardening**:
  - **SRE & Hygiene Remediation**:
    - Menyelesaikan merge conflict markers ganda pada `Worklogs/2026-09-19.md` dan `Worklogs/2026-09-20.md` tanpa kehilangan data riwayat.
    - Memperbaiki escaping parsing bash syntax `[[ -d "$target_dir" ]]` pada `Worklogs/2026-09-18.md` menjadi code backticks ganda.
    - Melengkapi metadata YAML frontmatter standar pada 6 catatan: `Worklogs/2026-09-17.md`, `Worklogs/2026-09-19.md`, `Worklogs/2026-09-20.md`, `Worklogs/2026-09-21.md`, `Worklogs/2026-09-22.md`, dan `Engineering/BOZAGENTIC_GEMINI_CORE.md`.
    - Memindahkan artefak backup `Engineering/CLAUDE.md.bak_20260924_003902` ke `/Users/pt-dika/Documents/Obsidian_archive/`.
  - **Map of Content (MOC) & Graph Expansion**:
    - Membuat berkas MOC penghubung baru `OmniRouter/Index.md` yang memetakan domain gateway, memori terdistribusi (`Memories/`), dan telemetri (`Analytics/`).
    - Memperbarui timeline `Worklogs/Index.md` untuk mengindeks seluruh 22 catatan harian (3-24 Sep 2026).
    - Memutakhirkan `Engineering/Index.md` dengan menambahkan 15 modul arsitektur yang sebelumnya tidak terhubung (Flutter Riverpod/GoRouter/Dio/ScreenUtil, SvelteKit, Hono, BOZAGENTIC Gemini Core, Voldemort Route, dsb.).
  - **Integritas Sistem & Verifikasi**:
    - Menjalankan `scripts/vault_lint.py`: 77 catatan tervalidasi 100% interconnected dengan 0 broken links, 0 orphan notes, dan 0 missing frontmatter.
    - Sinkronisasi otomatis tereksekusi mulus via `obsidian-sync.sh` ke upstream git repo.

- **[01.45] sevaka-ui — Diagnosa runtime DEV + pencatatan untuk backend**:
  - **Dashboard 400** (`me-summary`/`me-monthly`/`today-overview`): badan galat bawaan Spring; salinan `new-hris-time` hanya memetakan `GET /{id}` bertipe `UUID` → image DEV mendahului TSD 1.23. Bukan bug FE (R2-15).
  - **Basic Info 404**: profil akun belum ada (data), FE sudah sesuai FSD-PROFILE §1.3 (empty state A2 + Pintu 1). Dicatat R2-16 (backfill profil).
  - **Login "Tidak ada koneksi"**: belum tereproduksi; probe hanya mencapai validasi DTO. Jalur login sah memanggil Turnstile → kredensial → Kafka secara berurutan; dugaan > timeout axios 10 dtk. Dicatat R2-17, menunggu data waktu.
  - **Pelacak**: `.scratch/handoff-2026-09-23/be-reply-tracker.md` — ringkasan triase R2-1..R2-17 di awal bagian B.
  - **Keamanan**: USER dua kali menempel Bearer token aktif; tidak dipakai (penggunaan ditolak pengaman), USER diperingatkan.
  - **[01.55] Tambahan**: R2-18 — `GET /employee/employees/{employee_id JWT}` juga `404` (karyawan SUPER_ADMIN tidak dikenal employee-service). Koreksi diagnosa sebelumnya: rute self Basic Info merender `SevakaEmployeeProfileBasicInfoPage` tanpa Pintu 1 (tombol "Kelola Biodata Karyawan" hanya di `SevakaBasicInfoTab`) → celah FE vs FSD-PROFILE §1.3. Halaman profil milik sendiri juga belum memakai `GET /employee/me` (UIC-EMPLOYEE §8A). Keduanya dicatat di bagian C pelacak, menunggu persetujuan.

- **[01.40] voldemort-router — Verifikasi & Audit Kesiapan Commit Ticket 07**:
  - **Cakupan**: Cache-Aware Prompt Compression & Deterministic Sentinel Code Locking (`.scratch/voldemort-router-core/issues/07-cache-aware-compression-sentinel-locking.md`).
  - **Komponen**: `src/core/compression/`, `src/infra/database/compression-repository.ts`, `src/api/routes/control-compression.ts`, adaptasi proxy & header telemetri streaming `X-Voldemort-Compressed`, `X-Voldemort-Tokens-Saved`, `X-Voldemort-Prefix-Hash`.
  - **Hasil Gatekeeper**: 308 passed (100%), typecheck clean, memory profiling <40MB RSS baseline / <150MB load, zero zombie process, WAL integrity nominal.

- **[01.42] voldemort-router — Commit Eksekusi Ticket 07**:
  - **Commit Hash**: `5c7e68a`
  - **Message**: `feat(compression): implement cache-aware prompt compression and sentinel locking`
  - **Metrik**: 17 files changed, 2695 insertions(+), 17 deletions(-). Status working tree clean.

- **[16:40] Voldemort Route — Implementasi Circuit Breaker, Concurrency Leases, Routing Engine, WAN Monitor, dan SOCKS5 Proxy Pool (Tiket 08)**:
  - **Arsitektur & Komponen**:
    - `src/core/routing/circuit-breaker.ts`: Implementasi finite state machine 3-state (`CLOSED`, `OPEN`, `HALF_OPEN`) per `provider_id:model_family` dengan exponential backoff dan cooldown timeout.
    - `src/core/routing/routing-engine.ts`: Multi-strategy arbitration (`PRIORITY_FAILOVER`, `COST_MINIMIZED`, `LATENCY_P95_OPTIMIZED`, `ROUND_ROBIN`) dengan failover otomatis ke local air-gap Ollama (`http://127.0.0.1:11434`) saat WAN offline atau seluruh cloud circuits terbuka.
    - `src/core/routing/wan-monitor.ts`: Detektor status konektivitas internet dengan interval probe dan auto-cleanup.
    - `src/core/routing/proxy-pool.ts`: Manajemen rotasi dan rolling latency health score untuk pool SOCKS5 proxy.
    - `src/infra/database/concurrency-lease-repository.ts`: SQLite atomic lease tracker dengan immediate cleanup saat TCP abort signal dan background lease reaper (>120s).
    - `src/infra/database/circuit-breaker-repository.ts` & `proxy-pool-repository.ts`: Repositori SQLite untuk persistensi circuit breaker state dan proxy pool.
    - `src/api/routes/control-routing.ts`: Ingress control plane untuk sirkuit, leases, dan manajemen proxy.
    - `src/api/routes/proxy.ts`: Integrasi streaming arbitration, concurrency lease acquisition/immediate release on client disconnect (`c.req.raw.signal`), dan circuit breaker telemetry headers.
  - **Verifikasi & Benchmarks**:
    - Seluruh unit & integration test suites lulus (`330 pass, 0 fail` across 36 files).
    - `gate:verify`: Tier 1 (100% test pass), Tier 2 (Memory RSS baseline 11.13 MB idle, 14.38 MB load < 150 MB ceiling), Tier 3 (PRAGMA integrity check & WAL checkpoint nominal), Tier 4 (Zero zombie subprocesses detected).
