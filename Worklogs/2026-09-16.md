---
tags:
  - daily-worklog
  - journal
date: "2026-09-16"
---

# 🗓️ Worklog: Wednesday, 16 September 2026

> **Related**: [[Worklogs/Index|📓 Worklogs Index]] | [[Engineering/Index|⚡ Engineering MOC]] | [[Home|🌌 Home]]

## 📝 Activity & Engineering Notes

- **Auto-Rotating Proxy Gateway Deployment & Architecture Documentation**:
  - Engineered and deployed a zero-cost rotating proxy stack under `/home/voldemort/services/proxy-pool/` using Docker Compose, HAProxy 2.8, Tor 0.4.8, and Privoxy.
  - Configured single entrypoint on `http://127.0.0.1:8080` (TCP mode, transparent CONNECT tunnel, zero MITM/SSL certificate overhead).
  - Multiplexed 5 independent Tor instances with automated staggered circuit rotation and instant on-demand renewal webhook (`POST http://127.0.0.1:8088/rotate`).
  - Added real-time HAProxy telemetry dashboard at `http://127.0.0.1:8089/stats`. Verified live round-robin rotation and IP switching across multiple requests.
  - Authored comprehensive architectural specification and hybrid escalation playbook: [[Engineering/Auto_Rotating_Proxy_Pool_Architecture]]. Linked to [[Engineering/Index]].
- **Obsidian Vault Graph Hardening & Automated Hygiene Guardrail**:
  - Purged ghost 0-byte files (`SevakaCompanySettingsTable.md`, `tooling-typecheck-and-format-traps.md`, `Untitled.base`) and updated `.gitignore` against stray untracked templates.
  - Resolved all dangling broken wikilinks down to 0, provisioning standard architectural cards for [[Engineering/Sevaka_UI]], [[Engineering/Voldemort_VPS]], and [[Engineering/OmniRoute]].
  - Synced and stitched 9 unlinked engineering blueprints into [[Engineering/Index]] MOC, refreshed [[Home]] and [[Worklogs/Index]] navigation paths.
  - Built and deployed automated SRE hygiene watchdog (`scripts/vault_lint.py`) wired into both `.git/hooks/pre-commit` and background auto-sync (`obsidian-sync.sh`) to permanently block broken links or ghost files from reaching git. Verified 100% interconnected graph pass.

---

## 💻 `sevaka-ui` — AUTH wayfinder: Home/Dashboard diputus lalu dibangun

> ℹ️ Entri ini ditulis ulang: catatan aslinya (tiket 10/14/15) hilang ketika berkas worklog hari ini sempat lenyap dan dibangun ulang oleh sesi lain. Isinya direkonstruksi dari riwayat commit, bukan salinan tulisan asli.

**Commit hari ini:**

- `42615203 docs(auth): decide the Home/Dashboard shape, and record the UIC gap under it`
- `ef26a16d fix(dashboard): stop HOME showing invented data, and honour the switcher rule`
- `63df5545 feat(dashboard): wire HOME's two "Milik Saya" stat cards to their real endpoints`
- `27eaf2e8 docs(auth): ask the backend for #96's response body`
- `e1749fc1 fix(routes): stop seven Employee Self-Service menus landing on HOME`

**Tiket 10 — keputusan.** Tiga ronde grilling, 10 keputusan. Probe gerbang DEV menemukan **4 dari 5 kartu statistik HOME sudah ber-route** (3 panggilan; `today-overview` melayani 2 kartu), `employees/active-count` masih `404`. Pembelahan tiket ternyata sudah tertulis di `FSD:180` dan jatuh persis di batas kesiapan. 🔴 Temuan utama: **HOME nol punya kontrak UIC** — `UIC-001-AUTH-0.14` nol memuat kata "HOME", "dashboard", maupun kelima nama alamat; enumerasi peran "HR/manajemen" nol pernah dipecah jadi nama role. Copy ditulis FE + dicatat; daftar peran dikirim ke backend.

**Tiket 14 — pembersihan** (25 berkas, **+297/−1032**). 4 komponen dihapus (grafik recharts, tabel Lorem Ipsum, Quick Links, kartu Who's Off dengan 3 nama karyawan karangan berfoto `dino.png` di bawah tanggal masa lalu yang dipaku), 14 tombol mati dicabut, switcher perusahaan disembunyikan saat akses tunggal (`FSD:353`/`:361`). **Tes hijau ternyata mensertifikasi pelanggarannya** — `[ui-1b]` berjudul harfiah _"still renders the switcher when companyAccess has exactly one entry"_; dibalik, bukan dihapus, dan dibuktikan merah-dulu. Kali ketiga berturut pola ini muncul. Penghapusannya juga membongkar cacat config tingkat repo: `vite.config.ts` nol punya `test.include`, jadi Vitest menyapu `.kilo/worktrees/*` yang gitignored.

**Tiket 15 — lapis "Milik Saya" terpasang.** Kontrak TIME ternyata ada di repo: FSD menyematkan `TSD-001-TIME-1.30` yang nol ada, tapi `1.23` yang ada justru **revisi yang melahirkan** `#95`–`#97`. Badan `#96` nol pernah dicetak — ditangani type predicate fail-closed + dikirim ke BE. Dua cacat dicegah: `isPending` di React Query v5 memaku skeleton selamanya saat `enabled:false` (pakai `isLoading`), dan `projected_days` nol boleh tayang sebab nilai jatuh-baliknya nol bisa dibedakan dari hitungan sah.

**Tiket 17 — tujuh menu ESS berhenti mendarat di HOME.** Lingkup menyusut **9 → 7** karena keputusan peta tetangga: `.scratch/employee-integration/issues/07-*` sudah menetapkan kriteria _"dipancarkan menu-tree atau tidak"_ dan sudah menerapkannya. Buktinya dump `menuGroups` DEV nyata yang ternyata ada di repo — `.scratch/time-integration/research/03-menu-tree.json`, 154 `menu_url` — yang sekaligus sumber sah label menu, jadi judul halaman diambil verbatim dari `menu_name`. 🔴 Satu contoh di tiket sendiri ternyata karangan: label "Riwayat Mutasi" nol pernah ada di menu ESS (aslinya `Transfer`) — ditulis di tiket 10 sebagai ilustrasi, diwarisi tiket 17 sebagai fakta.

**Suite sepanjang hari:** 641 berkas / 3920 tes → **645 / 3963**, `npm run lint` exit 0 di tiap commit.

**Tersisa:** tiket 16 (lapis "Perusahaan") nunggu 2 balasan BE — enumerasi peran + route `employees/active-count`; tiket 13 nunggu balasan Q2; repository secret `VITE_TURNSTILE_SITE_KEY` masih wajib dibuat sebelum deploy dev berikutnya.

## 🔐 `sevaka-ui` — AUTH tiket 11: pengerasan sesi diputus (`f2bf2d58`)

Dua ronde grilling, 4 keputusan. **Antrean tiket keputusan peta AUTH habis (01–11)**; frontier tinggal tiket 12 (kirim dokumen handoff).

**Probe membalik strukturnya.** `POST /auth/force-logout` ternyata **ber-route** (gerbang diskriminatif dua arah: tanpa header → `Missing JWT token in request`, bearer sampah → `JWT token invalid`), sementara `POST /auth/logout` `404`. Pembenaran hack `sevaka_at` di initializer — _"no live /auth/logout route to revoke"_ — separuh benar: pencabutnya hidup dengan nama lain, dan UIC §3.5 mengontrakkannya. Jadi dua butir yang tiket pisahkan ternyata **satu masalah**: hack itu ada karena keluar nol mencabut, dan hack itu pula yang mengunci token di `sessionStorage`.

🔴 **Konsekuensi terukur yang tiket nol sebut: `sessionStorage` per-tab.** Keluar di tab A nol menyentuh tab B — pemakai yang menekan "Keluar" masih sepenuhnya masuk di tab sebelahnya, dan server nol pernah tahu. Kebalikannya juga: tab **baru** tampak keluar padahal cookie hidup 8 jam.

**Butir yang tiket kirim ke backend ternyata bisa diukur.** Tiga bentuk header ke `/auth/refresh` (nol header · `Bearer garbage` · JWT palsu berbentuk sah) menjawab **byte-identik** — klaim docblock _"APISIX requires Auth=Y"_ nol didukung. Header tetap **dipertahankan** karena asimetri risiko (probe nol pegang cookie sah, jadi cuma jalur gagal); yang diperbaiki komentarnya.

**Diputus:** tombol keluar memanggil `force-logout` (`SELF_LOGOUT` × `SINGLE_EMPLOYEE`), menunggu balasan, gagal = tetap masuk + pesan jujur. **Lalu** token pindah ke memori (menjawab UIC §10.4 butir 2 dengan rekomendasinya sendiri). Urutan itu **kontrak keselamatan**: memori-saja nol aman sebelum keluar mencabut. Lahir tiket 18 → 19.

Nol kode produksi berubah — tiket keputusan.

## 🚪 `sevaka-ui` — AUTH tiket 18: keluar mencabut sesi (`8086f9be`)

Tombol keluar kini memanggil `POST /auth/force-logout` (`SELF_LOGOUT` × `SINGLE_EMPLOYEE`), menunggu balasan, dan membersihkan state lokal **hanya** bila pencabutan berhasil. Gagal ⇒ tetap masuk + pesan jujur ("sesi belum ditutup, Anda masih masuk"). Suite **645 berkas / 3964 tes**.

**Kontrak dikunci di tipe, bukan di disiplin pemanggil.** UIC §3.5 punya 5 `reason` × 2 `scope`; `:502` cuma mengizinkan `SELF_LOGOUT` dari layar pengguna biasa. Jadi interface-nya nol memodelkan enum penuh — literal saja — dan `mutationFn` nol menerima parameter. Nol ada pintu untuk menyuntikkan `reason` lain.

🔴 **Dua temuan soal tes.** Pertama: tes existing berjudul harfiah _"calling logout twice triggers all effects twice (no internal guard)"_ mensertifikasi ketiadaan penjaga yang tiket ini tuntut — **pola keempat berturut** di seri ini (04, 05, 14, 18). Kedua, dan lebih penting: **versi pertama tes pengganti saya sendiri vacuous** — penjaga dicabut, tes tetap hijau 10/10, karena `mutate()` mencapai `mutationFn` di microtask sehingga asersinya balapan. Diperbaiki dengan membilas microtask, lalu dibuktikan ulang: hijau dengan penjaga, merah tanpa.

Pelajaran yang saya catat ke memori: **merah-dulu bukan formalitas — ia satu-satunya pembeda penjaga dari komentar.**

Ikut dikoreksi: klaim docblock _"APISIX requires Auth=Y on /auth/refresh"_ (nol didukung pengukuran) dan sitasi `ADR-0002` yang nol ada di repo.

Tiket 19 (token ke memori) **lepas blokir** — syarat keselamatannya terpenuhi.

## 🔑 `sevaka-ui` — AUTH tiket 19: token akses ke memori (`b95a15ad`)

Token akses kini hidup **hanya di memori**. `UIC-001-AUTH` §10.4 butir 2 — satu-satunya butir yang handbook tandai _"Belum diputuskan"_ dan _"berpotensi mengubah kode lama Frontend"_ — terjawab dengan rekomendasinya sendiri. Suite **645 berkas / 3966 tes**.

Dua cacat per-tab ikut sembuh: keluar kini merambat ke seluruh tab, dan tab baru memulihkan sesi alih-alih tampak keluar padahal cookie hidup 8 jam.

🔴 **Satu interaksi yang tiket nol sebut.** Memori-saja **memaksa** refresh jalur muat-ulang berangkat tanpa header `Authorization` — nol ada token untuk dikirim. Padahal tiket 11 baru memutuskan **menahan** header itu sampai BE menjawab. Resolusinya: keputusan lama tetap berlaku di tempat yang masih mungkin (`getRefreshAuthHeader()`, in-session); yang berubah cuma jalur yang fisik nol punya token. Bukti aman berdiri tiga kaki, dan kaki keempat justru absen — **provenans**: header itu masuk lewat commit refactor berpesan satu baris (`bd0a60f3`), nol catatan ia memperbaiki kegagalan nyata.

⚠️ Titik regresi yang perlu dilihat mata manusia di DEV: **muat ulang setelah masuk**.

**Sembilan tes** mensertifikasi perilaku lama. Yang paling menonjol judulnya mengeja alasan yang tiket 18 batalkan — dan alasan itu **benar pada saat ditulis**, jadi judul lamanya dikutip di tempatnya supaya jejaknya nol hilang.

Merah-dulu dibuktikan: menulis token kembali ke `sessionStorage` menggagalkan 3 tes.

**Seri build peta AUTH tinggal 2 tiket, dua-duanya menunggu balasan backend** (13 dan 16).

## 📨 `sevaka-ui` — AUTH tiket 20: balasan BE (MYTHOS) direkonsiliasi (`bbb77ccc`)

Analisa MYTHOS atas empat catatan FE mendarat 13:40 WIB. Ditelan, tiap klaim diadu ke dokumen yang **kita pegang**, lalu dijawab balik. Nol kode produksi.

**Delapan butir tertutup**: Q8 (prefiks `+62` memihak UIC ⇒ FSD §2.3 yang perlu dikoreksi), Q11, Q13, Q16, Q3 (gerbang kita diterima; yang usang cuma nama izinnya), Q10 (TTL OTP kini berujuk), konfirmasi `present_days` benar, konfirmasi route `active-count` belum dibuat. **Sembilan tetap kosong** — Q2 di antaranya masih menahan menu Unlock User.

🎯 **Koreksi terbesar milik saya sendiri.** Enumerasi peran lapis "Perusahaan" HOME ternyata **ada di salinan yang saya pegang sejak awal** — `TSD-001-TIME-1.23:11675`. Sapuan saya nyata tapi **lingkupnya salah**: saya menyapu FSD/UIC **AUTH**, padahal pemilik kontraknya TSD-TIME — dan tiket 15 sendiri sudah menetapkan itu, bahkan sudah membaca baris tetangganya. Akibatnya tiket 16 dipersempit jadi satu kartu, dua sisanya lahir sebagai tiket 21 yang **siap dikerjakan** tanpa menunggu redelivery.

Pelajarannya saya catat ke memori sebagai aturan: **sapuan negatif wajib menyebut lingkup yang disapu**, dan lingkupnya wajib mencakup pemilik kontraknya.

**Satu koreksi balik ke BE**: badan respons `#96` **belum** tercetak di `1.23` — tanda "baru `1.23`" menempel pada **alamatnya**, bukan badannya (sapuan ketiga, tetap 0).

🔴 **Satu permintaan BE ditolak, beralasan.** Mereka minta jangan turunkan penjaga amplop ke 3 kunci. Ditolak sebab `unwrapApiEnvelope` **pembongkar bersyarat, bukan validator** — penjaga gagal ⇒ muatan diteruskan utuh, bukan ditolak. Jadi `/auth/me` telanjang sudah berperilaku persis seperti yang mereka minta, dan menuntut `timestamp` cuma bikin lebih banyak bentuk **sah** gagal dibongkar. Plus: analisa mereka nol menyentuh `PROB-SERVICE-291` (31 Juli) yang membekukan amplop 3-kunci untuk **tiga** alamat lintas service. Diminta keputusan eksplisit `291` vs `195` untuk ketiganya.

## 🏁 `sevaka-ui` — AUTH tiket 12 ditutup: frontier peta kosong (`7854d85e`)

Premis tiket ini bergeser **tiga kali**, dan yang ketiga terjadi **sehari sebelumnya**: dokumen handoff bukan cuma sudah ada — ia **sudah dikirim dan sudah dibalas** MYTHOS. Jadi yang tersisa tiga butir administratif.

**Dua nol perlu dikerjakan ulang.** "Mana dari Q1–Q16 yang gugur" sudah divonis MYTHOS terhadap SoT terakhir. Dan pertanyaan penomoran ternyata punya jawaban **empiris**: tiga pertanyaan 16 September ditulis sebagai blok bertopik-bertanggal **tanpa nomor**, dan ketiganya dibalas — di tabel tersendiri. Yang dibutuhkan agar dibalas itu **topik yang bisa disebut**, bukan indeks. Renumbering ditolak: tiap rujukan "Q11"/"Q16" di catatan, memori, dan balasan BE akan menggantung.

🔴 **Butir ketiga nyata, dan itu temuan tiket ini: kewajiban FE `UIC §10.4 butir 1` nol pernah ditunaikan.** Handbook minta konfirmasi **tertulis** dari FE sebelum jalur token cookie/URL ditutup; dokumen handoff nol memuat satu pun kalimatnya, padahal butirnya nongkrong di fog peta sejak charting.

Ditunaikan, dengan klaimnya **diukur lebih dulu**: `document.cookie` → 0, `[?&]jwt=` → 0, pustaka cookie → 0. Token menempel satu bentuk saja.

Disertai peringatan yang bobotnya naik justru karena tiket 19: `withCredentials` membawa `hris_rt`, dan sejak token akses memori-saja, `hris_rt` jadi **satu-satunya** penentu sesi hidup — menyentuhnya saat menutup §9 butir 12 mematahkan **seluruh pemulihan sesi**, bukan satu permintaan.

**Peta AUTH: seluruh tiket keputusan + task tutup (01–12, 20). Frontier kosong.** Sisa cuma build — tiket 21 siap gas, 13 & 16 nunggu balasan BE.

## 📊 `sevaka-ui` — AUTH tiket 21: kartu lapis "Perusahaan" HOME (`4ace94ed`)

Kartu "Hadir Hari Ini" dan "Sedang Cuti Hari Ini" terpasang. **Empat dari lima kartu `FSD:180` kini tayang.** Suite 645/3966 → **647/3999**.

**Dua kartu, satu panggilan.** `#97` memberi `present_count` + `on_leave_count` dalam satu respons (`INS-30` D4), jadi kedua kartu memanggil **hook yang sama** dan React Query mendedupe-nya jadi satu permintaan. Mengoper data lewat props dari induk ditolak — itu memindahkan kepemilikan kueri ke komponen yang bukan pemiliknya.

**Dua jebakan ditutup oleh konstanta, bukan oleh kehati-hatian.** Nama peran diambil dari `SEVAKA_ROLE`, nol ditulis literal — klaim `role` selalu ber-prefiks `ROLE_`, dan bentuk tanpa prefiks bikin penyaring **diam-diam selalu false** (pernah kejadian di **delapan tempat** di modul Time). Dan Dept Manager punya **dua ejaan** di korpus, jadi `SEVAKA_DEPT_MANAGER_ROLES` menampung keduanya — milih satu ejaan mematikan gerbang untuk separuh kasus. Tes mengunci ketiganya.

Gerbang peran duduk **di dalam kartu**, bukan di tempat pemasangan (preseden tiket 04) — gerbang di induk tertinggal begitu komponennya dipasang di tempat kedua.

Merah-dulu dibuktikan **dua kali**: cabut gerbang (kartu + `enabled`) → 6 tes gagal; pecah `queryKey` → penjaga "satu panggilan" gagal.

**Peta AUTH: seluruh tiket keputusan, task, dan build-yang-bisa-dikerjakan tutup.** Sisa dua — 13 (Q2) dan 16 (route `active-count`) — dua-duanya menunggu backend, nol satu pun menunggu kita.
