---
tags:
  - daily-worklog
  - journal
date: "2026-09-19"
---
# 🗓️ Worklog: Saturday, 19 September 2026
> **Related**: [[Worklogs/Index|📓 Worklogs Index]] | [[Engineering/Index|⚡ Engineering MOC]] | [[Home|🌌 Home]]

- **OmniRoute - Ingestion 100 Grok OAuth Accounts**:
  - Diimpor secara otonom 100 akun OAuth Grok dari arsip ZIP ke provider `grok-cli` di database `storage.sqlite`.
  - Mengimplementasikan enkripsi AES-256-GCM field-level yang kompatibel dengan key derivation runtime OmniRoute (`STATIC_SALT`).
  - Pool terkonfigurasi dengan status `active`, priority 1, auto-rotation, dan lazy token refresh. Service OmniRoute direstart dan healthy di port 20128.

- **OmniRoute - Batch 2 Ingestion (105 Grok OAuth Accounts)**:
  - Berhasil menambahkan 105 akun tambahan dari arsip ZIP kedua ke pool `grok-cli` di database `storage.sqlite`.
  - Menggunakan enkripsi AES-256-GCM field-level identik, status active, flat priority 1, dan zero duplication.
  - Total pool provider `grok-cli` kini mencapai 205 akun. OmniRoute direstart dan healthy.

- **Web Wedding Planner - Container Deployment & SRE Remediation**:
  - Investigasi dan remediasi root cause 502 Bad Gateway pada domain `wedding-plan.portojssi.net`.
  - Mengoreksi multi-stage `Dockerfile` (Node 20 + Composer dependencies injection untuk Ziggy + PHP 8.4 runtime).
  - Melakukan build container `wedding-planner-app`, inisialisasi `key:generate`, migrasi database ke container `mysql-service`, dan verifikasi reverse proxy Nginx + TLS Certificate Let's Encrypt (HTTP 302 -> 200 OK).
  - Diagnosa insiden blank screen (Mixed Content Block W3C): Menyusun dokumen PDF Incident RCA & Runbook (`Laravel_Blank_Screen_Root_Cause_Fix_Guide.pdf`) tanpa mengubah kode aplikasi secara langsung sesuai arahan pengguna.

- **Zshrc NVM Eager Loading**: Transisi konfigurasi NVM pada `/Users/pt-dika/.zshrc` dari lazy-loading wrapper (`load_nvm`) ke canonical eager sourcing (`nvm.sh` & `bash_completion`). Eliminasi manual static path injection di `target_paths`. Verifikasi subshell `zsh -i` sukses (`nvm 0.40.1`, `node v22.22.0`).

- **Attendance Dead-Surface Audit (sevaka-ui, `396c6541`)**: Tiket audit `06` seri attendance-reskin ditutup. Tujuh permukaan Attendance (Filters, Activity, Deduction, Import/Export, dua modal Report, butir menu data-locking) terbukti nol berkontrak di `FSD-001-TIME-0.3` + `UIC-001-TIME-0.8` — `FSD:80-92` mencacah 11 Menu Time, domain Attendance hanya pegang #5 dan #6. Konsekuensi dipecah jadi tiket `07`/`08`/`09`/`10` menurut kopling impor, seluruhnya tetap `needs-triage` (keputusan hapus milik produk).
- **Tiga premis tiket terbalik saat verifikasi**: `window.confirm` di jalur tarik koreksi ternyata **dikontrakkan** FSD tiga kali (`:972`/`:1079`/`:1144`), prototipe yang menyimpang — dipertahankan plus docblock penyitir. Butir menu `Location`/`Settings` berkontrak tapi di Menu #6, layarnya sudah tayang di `routes.tsx:419`. Sebelas klaim `TIME-GAP-NOTES` prototipe diverifikasi: nol berlaku sebagai cacat di repo ini.

- **Maestro Squad Matrix Hardening: Deep Engine & Sovereignty Protocol untuk Agen `query`**:
  - **Context & Goal**: Menuntaskan arsitektur agen `query` agar memiliki pondasi yang hebat, kokoh (*robust*), tahan banting (*bullet-proof*), dan berorientasi jangka panjang (*future-proof*) tanpa celah (*zero gap*) dalam domain MySQL, PostgreSQL, dan SQLite.
  - **Changes Made**:
    - **Engine Storage Mechanics**: Spesifikasi mendalam untuk PostgreSQL (Heap/MVCC, HOT updates via fillfactor, non-blocking DDL dengan `lock_timeout`, GIN/GiST/BRIN), MySQL InnoDB (Clustered B+Tree PK, composite covering index tanpa bookmark lookup, eliminasi gap/next-key lock, eliminasi disk temporary tables & filesort), dan SQLite (WAL mode single-writer concurrency, busy timeout 5000ms, Stored Generated Columns, EXPLAIN QUERY PLAN).
    - **Pragmatic 80/20 & Anti-Overengineering Guardrails**: Larangan abstraksi kompleks prematur jika latensi in-memory cache < 2ms; volume guard (dilarang partitioning/sharding < 5-10M baris); index bloat guard (maksimal 3-5 indeks pada tabel high-write).
    - **Transaction & Concurrency Hygiene**: Larangan keras external I/O (HTTP/disk/API) dalam blok transaksi, shortest transaction duration, statement timeouts, 100% prepared statements (zero string concatenation / zero SQL injection).
    - **Clean Repository / DAO Handoff**: `query` merancang dan menulis typed repository layer / DAO interfaces; `builder-backend` strictly hanya memanggil methods dari repository tanpa celah menyusun kueri string.
    - **Reviewer Dual-Sided Audit Gatekeeper**: Penegakan checklist audit ketat (Sovereignty Check: instant REJECT jika non-query agent menulis kueri/DDL; Injection Safety: 100% parameterized; Transaction Scope: zero external I/O; Performance Proof: validasi EXPLAIN).
    - **Runtime & Docs Sync**: Terintegrasi ke `~/.config/kilo/kilo.json`, `Engineering/AGENTS.md`, dan `Engineering/Maestro_Orchestration_Engine.md`.

- **AquaSync Admin & Operator Setup (`aquasync`, `2026-09-19`)**:
  - Inisialisasi akun Superuser PocketBase `admin@aquasync.id` (`POST /api/admins`) untuk akses web dashboard `http://127.0.0.1:8090/_/`.
  - Registrasi akun operator mobile pada collection `users`: `admin@aquasync.id` dan `admin01@email.com` dengan password `AquaSyncAdmin123!`.
  - Konfigurasi ADB reverse port forward (`adb reverse tcp:8090 tcp:8090`) untuk konektivitas mobile client ke backend lokal. Seluruh pengujian otentikasi REST API dan audit keamanan diverifikasi `PASS`.

- **Attendance Dead-Surface Deletion (sevaka-ui, `29df218a`→`151a804e`)**: Keputusan produk turun (hapus + jalan A), keempat tiket konsekuensi dijalankan berurutan 07→09→10→08. Tujuh permukaan nol-kontrak dicabut berikut `SevakaBaseFilterModal` dan dua Yup schema — **26 berkas, −2.979 baris**. Layar Attendance kini tinggal judul, bilah tiga tab, dan ketiga tab berkontrak. Seri `.scratch/attendance-reskin/` tutup, 10/10 tiket `resolved`.
- **`/code-review` dua lensa konvergen ke satu cacat**: commit `d1404819` ikut menghapus penjaga tes `.is-on` yang subjeknya masih hidup. Sebabnya `re.S` pada regex penghapus bikin `.*` di grup docblock melintasi baris. Cacah AC tiket itu sendiri sudah menandainya (minta turun 1, turun 3) tapi dibaca sebagai wajar. Dipulihkan di `151a804e` dan dibuktikan nol vakum lewat mutasi.
- **Baseline suite penuh diukur ulang**: 12 kegagalan di `HEAD` ternyata **juga 12 di `b850080f`** (pra-kerja), seluruhnya klaster `employee-profile` yang lulus bila dijalankan sendirian. Cacah suite penuh di repo ini nol stabil (2–12 pada commit sama) — bukan sinyal regresi tanpa baseline pembanding.

## 📝 Engineering Notes & Scratchpad
- **[12.26] Claude Code Session (`sevaka-ui`)**:
  - **Working Directory**: `/Users/pt-dika/Dika/sevaka-ui`
  - **Recent Commits**:
    - `151a804e test(attendance): restore the .is-on tab-marker guard deleted by mistake`
    - `3c298596 docs(attendance): record the dead-surface decision and close the series`
    - `e5858cff refactor(attendance): drop the three-dot menu from the Attendance header`
    - `70897cf2 refactor(attendance): delete the Attendance Report button and its modals`
    - `d1404819 refactor(attendance): delete the Absence-Time Off Deduction sub-view`

- **AquaSync Mobile Dev Flavor & Smart Host Resolution**:
  - Implementasi dynamic loopback rewrite pada `mobile/lib/core/pocketbase_client.dart` (`127.0.0.1`/`localhost` -> `10.0.2.2` khusus Android) dengan hierarki precedence: `--dart-define` > `dotenv` > fallback default.
  - Penegakan git hygiene pada `mobile/.gitignore` (`.env*` di-ignore, `!assets/env/*.example` dipertahankan).
  - Penghapusan `mobile/assets/env/.env.dev` dari git tracking cache sembari mempertahankan file fisik lokal, serta penambahan template `mobile/assets/env/.env.dev.example`.
  - Verifikasi otomatis: 9 skenario unit test baru, `make mobile-analyze` lolos (0 issues), `make mobile-test` 100% pass (27/27 tests).

- **AquaSync Mobile Auth & flutter_screenutil_plus v1.6.0 Standardisation**:
  - Refactor `mobile/lib/features/auth/presentation/login_screen.dart` mematuhi `[[Flutter_ScreenUtil_Plus_Performance_Standard]]`: eliminasi 100% hardcoded raw double pixels, preservasi `const RSizedBox.vertical`/`horizontal`, dynamic context extensions (`context.w()`, `context.h()`, `context.r()`), defensive typography `context.spMin()`, dan accessible touch target >= 48dp pada checkbox remember device.
  - Bersihkan redundansi `Timer.periodic` di `mobile/lib/features/auth/presentation/server_health_provider.dart`.
  - Update `mobile/test/auth_login_test.dart` dengan test harness `ScreenUtilPlusInit` dan helper `setupViewport(tester)` (390x844, DPR 3.0). Seluruh test PASS (8/8) dan `flutter analyze` zero warning/error.


- **Comprehensive SRE Audit & Self-Healing Hardening on `voldemort-vps`**:
  - **Host & Containers**: Uptime 3+ days, CPU load normal (0.5-0.7 / 2 vCPU), 0 OOM kills, RAM 2.0GB/3.8GB used, root disk 59% (24GB available). All 6 production containers (`omniroute`, `mysql-service`, `postgres-service`, `voldemort-menu-bot`, `dozzle-service`, `obsidian-sync`) running healthy with restart policy `unless-stopped` and memory limits enforced.
  - **Hermes Gateway & OmniRouter Connectivity**: `hermes-gateway.service` active and healthy with cgroup limits (`MemoryMax=1.5G`, `Restart=always`). Model binding `AG` / `Agnes` verified via `http://127.0.0.1:20128/v1` with 200 OK upstream completions. Telegram network latency optimal (170ms roundtrip to `api.telegram.org`).
  - **Hardening & Sentinel Watchdog**: Cleaned 2.18GB dangling Docker build cache. Hardened `/usr/local/bin/sre-sentinel.sh` (timestamped backup `.bak.20260919085232`) to include `mysql-service` and active health checking for `hermes-gateway.service` with automatic restart and Telegram alert triggers.

- **Pre-Departure VPS SRE Audit (voldemort-vps)**:
  - Host telemetry: Load avg 0.56, RAM 2.0/3.8 GiB (Available 1.8 GiB), Disk 24 GB available (59%), 0 OOM events.
  - Containers: 6/6 healthy (`omniroute`, `mysql-service`, `postgres-service`, `voldemort-menu-bot`, `dozzle-service`, `obsidian-sync`).
  - Hermes Gateway: Running via systemd unit `hermes-gateway.service` (uptime 3d 18h, RSS 360.7M, cgroups limit 1.5G, restart=always). Telegram polling 0 conflict/0 timeout (`api.telegram.org` latency 24.3ms).
  - OmniRouter: Combo `AG` (7/7 accounts active) dan `Agnes` (8/8 accounts active) teruji 200 OK dengan latency < 350ms. Zero stopper detected.
